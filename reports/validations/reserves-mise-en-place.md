# Validation — Levée des réserves R1 à R7

- Date : 2026-09-18 00:28 · Itération : 1 · Validateur : qa-validator
- Objet : réserves R1 à R7 du rapport `reports/validations/preparation-mise-en-place.md` (itération 2, verdict VALIDÉ AVEC RÉSERVES), après fusion de la pull request #1 dans `main` (merge commit `22d029d`, décision humaine G2) et corrections J-20260918-001 à 006.
- Contexte git : branche `chore/reserves-verification`, créée depuis `origin/main`, aucun commit local ; 12 fichiers en attente de commit.
- Verdict proposé : **VALIDÉ AVEC RÉSERVES** — **les 7 réserves sont levées**. Un seul défaut nouveau, documentaire et mineur (**N2** : « 11 autres bassins » alors qu'il y en a 10), non bloquant pour la pull request.

Méthode : commandes lancées depuis la racine avec `venv/Scripts/python.exe` ; git en lecture seule (`log`, `show`, `status`, `check-ignore`, `ls-files`, `rev-parse`, `branch --show-current`). Le rapport précédent n'a pas été modifié.

## Réserves

| # | Réserve (itération 2) | Méthode / commande | Observé | Verdict |
|---|---|---|---|---|
| R1 | `data/raw/` et `IoTpond1.csv` absents (humain, G4) | `hashlib.sha256` sur le fichier ; `csv.reader` pour l'en-tête et le comptage ; comparaison à `config.RAW_COLUMNS` et à `docs/01` ; `git check-ignore -v` ; `git status` | Empreinte **recalculée par mes soins** : `063ea4f0c9fcd24f016fbfc52e5422b655dfb4f3542d40476d99d899655dda76`, identique à celle annoncée et à celle consignée dans `docs/01`. Taille 6 809 242 octets (6,8 Mo). **83 126 relevés** hors en-tête, conforme à `docs/01`. **11 colonnes**, en-tête strictement égal à `config.RAW_COLUMNS` (ordre compris, 0 écart). Premier relevé : `2021-06-19 00:00:05 CET,1889,…` → suffixe « CET » et plage de dates confirmés. Autres bassins présents : **10** fichiers (`IoTPond2, 3, 4, 6, 7, 8, 9, 10, 11, 12` — `IoTPond5.csv` est absent), hors périmètre du MVP (ADR-001) ; `docs/01` et `docs/11` en annoncent 11 → défaut **N2**. Tous les fichiers de `data/raw/` sont ignorés (`.gitignore:11:data/raw/*`), et aucun n'apparaît dans `git status`. | ✅ **Levée** (défaut N2 à part) |
| R2 | Défaut littéral `log_path = "logs/decisions.log"` (D6) : arbitrer `docs/03` ↔ conventions, puis aligner code et doc | `git diff src/decision_engine.py tests/test_decision_engine.py docs/03-ARCHITECTURE_CODE.md` ; comparaison `ast` + `inspect.signature` de `docs/03` au code ; recherche de littéraux de chemin dans `src/` et `dashboard/` | Signature réelle : `log_decision(action: 'dict', log_path: 'str \| Path \| None' = None) -> 'None'`, **identique** à celle de `docs/03` (contrôle automatique : 19 fonctions documentées, 18 identiques, 1 équivalente en valeur, 0 divergence). `None` = chemin par défaut résolu depuis `config.DECISIONS_LOG_PATH` (import différé), documenté dans la docstring et dans `docs/03`. Le balayage `ast` des littéraux de chemin dans `src/` et `dashboard/` ne renvoie plus que `prog="src/main.py"` (libellé d'aide d'argparse, pas un accès fichier). Test réel ajouté : `test_log_decision_default_log_path_is_not_a_literal_path` (défaut `is None` par `inspect.signature`). Corps toujours `NotImplementedError` référencé (Jalon 4). | ✅ **Levée** |
| R3 | Compteurs et état de `docs/11` et `README.md` | `venv/Scripts/python.exe -m pytest -q` ; lecture et `git diff` de `docs/11` et `README.md` | `pytest -q` → **`32 passed, 38 skipped`**, 0 échec. `docs/11` l. 8 et `README.md` l. 9 annoncent « 32 tests réussis, 38 prévus en `skip` » : **cohérent**. État à jour : PR #1 fusionnée (merge commit `22d029d`, 2026-09-17T23:13:11Z), prérequis `data/raw/` et PR #1 passés à « Levé », D6 retiré des « Décisions en attente » (résolu par R2), tableau des réserves avec statut et preuve, « Prochaine action » alignée sur la PR de levée des réserves, le Jalon 1 et la replanification (G3), ligne d'historique ajoutée. `docs/08` : R1 (délai) complété — Jalon 1 non démarré le 2026-09-17, replanification non tranchée. Reste l'erreur N2, reprise du prérequis `data/raw/`. | ✅ **Levée** |
| R4 | Heure des entrées du doc-keeper (sans shell) lue avant sa dernière écriture | Comparaison de l'en-tête de J-20260918-006 aux horodatages de `actions.jsonl` | Dernières écritures de documents du doc-keeper : `docs/11` 00:21:19, `README.md` 00:21:31, `docs/08` **00:21:42** ; entrée de journal écrite à **00:22:10**. L'entrée cite `ts = 2026-09-18T00:21:42` — c'est bien la dernière ligne **postérieure** à sa dernière écriture de document, comme l'exige la règle corrigée. Écart en-tête (00:21) / écriture réelle (00:22:10) : **28 s**, contre 2 min à l'itération 2. Même méthode correctement appliquée en J-20260917-020. Les autres agents, qui disposent d'un shell, restent dans la minute (J-003 00:16 pour 00:16:12 ; J-005 00:16 pour 00:17:08 ; J-007 00:22 pour 00:22:36). | ✅ **Levée** |
| R5 | `RANDOM_STATE = 42` à confirmer par son propriétaire | `git diff src/config.py` ; `grep -rn -E "random_state\|RANDOM_STATE\|seed *=\|np\.random\|random\.seed\|default_rng\|sample\("` sur `src`, `tests`, `dashboard` | Valeur **inchangée** (42), confirmée par la ml-engineer dans sa propre section, avec un commentaire qui la qualifie de « graine arbitraire mais fixe (aucune portée scientifique) », précise les composants qui devront la recevoir explicitement (`IsolationForest`, `RandomForestClassifier`, `permutation_importance`, injection d'anomalies), rappelle que `train_test_split` n'est pas utilisé (split temporel) et que la reproductibilité sera vérifiée aux Jalons 3 et 6. **Unique graine du projet** : la recherche ne renvoie que `src/config.py:111` comme définition ; les autres occurrences sont des renvois en docstring (`src/models/*.py`) ou des tests (`tests/test_config.py:155`, `tests/test_models.py:55`). Renvoi ajouté dans `docs/03`. | ✅ **Levée** |
| R6 | Convention d'écart signé de `add_threshold_distance` à préciser | `git diff src/features.py tests/test_features.py docs/03-ARCHITECTURE_CODE.md` | La docstring fixe les 5 points demandés : **signe** (positif = marge avant le seuil critique, négatif = dépassement, valeur absolue = ampleur, zéro = valeur au seuil) ; **unité** identique à la colonne d'entrée, sans conversion ni normalisation ; **bornes** (borne basse seule : `valeur - critical_min` ; borne haute seule : `critical_max - valeur` ; double borne : distance signée à la borne la plus proche, avec les trois cas explicités) ; **seuils `None`** → colonne non produite (`turbidity`), de même pour `dissolved_oxygen`, `ammonia` et `nitrate` tant que l'unité n'est pas tranchée (A1), plutôt qu'une colonne `NaN` ; **nommage** `<clé>_distance_critical` à partir de la clé de `thresholds`. Convention reprise dans `docs/03` et dans le motif du test `skip`. Corps toujours `NotImplementedError` ; A1 n'est pas tranchée ; `src/config.py` non modifié par cet agent. | ✅ **Levée** |
| R7 | Conformité des commits fusionnés | `git log origin/main --format=…` (lecture seule), `git show -s --format=%B`, `git log --name-only`, `%(trailers:key=…)`, comparaison des références de journal aux fichiers `logs/agents/journal/*.md`, `git cat-file -s` | **11 commits** hors fusion (`9c4923b` → `5d85302`) plus le merge commit `22d029d` à deux parents (fusion par merge commit, conforme à l'ADR-007). Pour les 11 : format Conventional Commits valide, **types** (`docs`, `chore`, `build`) et **portées** (`cadrage`, `gouvernance`, `git`, `deps`, `structure`, `readme`, `suivi`) tous dans la liste de `git-workflow.md` §3 ; première ligne de **39 à 70 caractères** (limite 72), initiale minuscule, sans point final. **Trailers `Agent`, `Jalon`, `Journal` présents sur les 11** ; `Jalon: preparation` partout ; `Refs` présent dès qu'un ADR, un risque ou une section de spec est concerné (9 commits sur 11). **40 références de journal au total, toutes existantes** (les trailers repliés sur deux lignes sont bien lus par git, vérifié avec `%(trailers:key=Journal,valueonly)`). Cohérence message ↔ contenu vérifiée commit par commit : `Agent` correspond au propriétaire réel des fichiers (ex. `d2dc0e3` ml-engineer → `src/models/`, `models/`, `reports/experiments.md`, `tests/test_models.py` ; `707fb48` dashboard-developer → `dashboard/app.py`, `tests/test_dashboard.py` ; `b7dab64` orchestrateur/doc-keeper/qa-validator → `docs/11`, journaux, `reports/validations/`). **68 fichiers** commités, **aucun fichier interdit** (aucun `data/raw/`, `venv/`, artefact régénérable, `*.joblib`), **aucun > 5 Mo** (le plus gros : `logs/agents/actions.jsonl`, 157 106 octets). Deux écarts mineurs, sans effet : une ligne de corps à 73 caractères dans `b338fd1` (limite 72) et un contrôle réalisé **après** la fusion alors que l'échéance était « avant fusion » (O1, O2). | ✅ **Levée** |

## Contrôles transverses

- **Tests :** `venv/Scripts/python.exe -m pytest -q` → `32 passed, 38 skipped in 7.33s`, 0 échec. Analyse `ast` : 32 tests actifs, tous avec au moins un `assert` ou `pytest.raises`, aucun trivial ; 38 `skip`, chacun rattaché à un jalon et à une règle ou un critère.
- **Traçabilité :** `audit_trace.py --date 2026-09-17` → ✅ CONFORME (21 entrées, 144 actions) ; `--date 2026-09-18` → ✅ CONFORME (7 entrées, 59 actions, 0 échec ou refus, 0 fichier non cité, 0 entrée mal formée). Tous les agents ayant agi le 18/09 ont leur entrée (le qa-validator via J-20260918-008).
- **Contrats et squelette :** 19 signatures conformes à `docs/03` (18 identiques, 1 équivalente en valeur pour `add_rolling_features`) ; les 16 fonctions métier et `run_pipeline` lèvent toujours `NotImplementedError` avec une référence. Aucune logique métier introduite par les corrections.
- **Cohérence config ↔ docs :** `THRESHOLDS docs/03 == config : True`, extrait identique caractère pour caractère ; `TEMPERATURE_BOUNDS` et `PH_BOUNDS` conformes à `docs/01` et au cahier §5. Aucune décision ouverte tranchée (A1 à A5, ADR-003, fuseau, interpolation, ré-échantillonnage, anti-oscillation restent ouvertes).
- **Valeurs en dur :** plus aucun chemin littéral dans `src/` ni `dashboard/`, hors le libellé `prog="src/main.py"` d'argparse.
- **Périmètre :** aucune fonctionnalité hors cahier §3 ; les 10 bassins supplémentaires de `data/raw/` sont explicitement documentés comme hors périmètre et restent ignorés par git.
- **Git et fichiers à commiter :** branche `chore/reserves-verification` (≠ `main`), `git log origin/main..HEAD` vide (aucun commit local). 12 fichiers en attente : 11 modifiés (`README.md`, `docs/01`, `docs/03`, `docs/08`, `docs/11`, `logs/agents/actions.jsonl`, `src/config.py`, `src/decision_engine.py`, `src/features.py`, `tests/test_decision_engine.py`, `tests/test_features.py`) et 1 nouveau (`logs/agents/journal/2026-09-18.md`) — cohérents avec les réserves traitées. Aucun fichier interdit, aucun > 5 Mo (le plus gros : `logs/agents/actions.jsonl`, 184 639 octets), toutes les fins de ligne en LF, aucun jeton ni clé privée.

## Défauts à corriger

| # | Défaut | Preuve | Agent propriétaire | Bloquant ? |
|---|---|---|---|---|
| N2 | « `data/raw/` contient aussi **les 11 autres bassins** … (`IoTPond2.csv` à `IoTPond12.csv`) » : il y en a **10**, `IoTPond5.csv` étant absent du dossier. Même compte erroné dans `docs/11` (ligne de prérequis `data/raw/`) et dans l'entrée J-20260918-001. Correction attendue : « 10 autres bassins (`IoTPond2` à `IoTPond12`, `IoTPond5` absent) » dans `docs/01` et `docs/11`, plus une entrée de journal corrigeant J-20260918-001. | `ls data/raw/` → 11 fichiers au total : `IoTpond1, IoTPond10, IoTPond11, IoTPond12, IoTPond2, IoTPond3, IoTPond4, IoTPond6, IoTPond7, IoTPond8, IoTPond9` | doc-keeper (`docs/01`, `docs/11`) ; orchestrateur (entrée correctrice) | Non — à corriger avant l'ouverture du Jalon 1, `docs/01` y étant la référence du data-engineer |

## Observations

- **O1 :** le contrôle des messages de commit (R7) a eu lieu **après** la fusion de la PR #1, alors que l'échéance fixée à l'itération 2 était « avant fusion ». Résultat conforme, donc sans conséquence ; pour les jalons suivants, placer ce contrôle avant la décision G2.
- **O2 :** une ligne de corps de `b338fd1` fait 73 caractères, pour une limite de 72 (`git-workflow.md` §3). Historique publié, immuable : rien à faire, à surveiller sur les prochains commits.
- **O3 :** l'arbitrage `docs/03` ↔ `conventions-code.md` (R2) a été tranché par l'orchestrateur en faveur des conventions, sans ADR, et tracé dans J-20260918-003 ; la documentation a suivi dans le même cycle. Acceptable : il s'agit d'un défaut de signature, pas d'un seuil scientifique. À confirmer par l'humain s'il souhaite en garder une trace décisionnelle dans `docs/07`.
- **O4 :** `docs/11` classe R4 « en surveillance » ; je la considère **levée** au vu de J-20260917-020 et J-20260918-006, tout en gardant le point sous contrôle à chaque jalon.
- **O5 :** la réserve R7 était rattachée à la PR #1 ; les 12 fichiers de la présente branche ne sont pas encore commités, donc les messages de la future PR de levée des réserves restent à contrôler avant la fusion (G2).

## Points à trancher par l'humain

1. **G2 :** fusion de la pull request de levée des réserves, une fois les 12 fichiers commités et leurs messages contrôlés (O5), le défaut N2 pouvant être corrigé dans la même pull request.
2. **G3 — replanification :** le Jalon 1 n'a pas démarré le 2026-09-17 comme le prévoit l'ADR-006 (`docs/08`, R1) ; la nouvelle date de référence n'est pas tranchée, alors que le projet reste cadré sur 7 jours.
3. **R14 :** activer la protection de la branche `main` côté GitHub reste recommandé, le garde-fou `guard_git.py` étant local.
