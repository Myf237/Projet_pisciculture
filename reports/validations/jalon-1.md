# Validation — Jalon 1 : Données nettoyées et fiables

- Date : 2026-09-18 07:48 · Itération : 1 · Validateur : qa-validator
- Branche : `feat/jalon-1-donnees-nettoyees` · Livrables examinés : `data/processed/pond1_clean.csv`, `reports/cleaning_report.json`, `src/ingestion.py`, `src/config.py`, `tests/test_ingestion.py`, `tests/test_config.py`
- Références : `docs/04-JALONS_VALIDATION.md` (Jalon 1), `docs/02-SPEC_TECHNIQUE.md` §2 et §8, `docs/01-DATA_DICTIONARY.md`, ADR-003, ADR-009, ADR-010, `reports/analyse-donnees-jalon1.md`
- Environnement de vérification : `venv/Scripts/python.exe` (pandas/numpy), toutes les mesures recalculées par le validateur sur les fichiers eux-mêmes, sans réutiliser les chiffres du data-engineer
- **Verdict proposé : VALIDÉ AVEC RÉSERVES**

Les cinq critères de `docs/04` sont satisfaits sur les données ; la réserve principale ne porte pas sur le nettoyage mais sur la **justification écrite du seuil ammoniac** (ADR-003), qui décrit les données autrement que ce qu'elles sont. Deux réserves de fond concernent la suite : l'ampleur des données manquantes après nettoyage et l'absence de point d'entrée de régénération du livrable.

---

## 1. Critères du Jalon 1 (`docs/04-JALONS_VALIDATION.md`)

| # | Critère (docs/04) | Méthode / commande | Observé | Verdict |
|---|---|---|---|---|
| 1 | Aucune valeur de température hors [0, 40] °C | Recalcul sur le livrable : `pd.read_csv("data/processed/pond1_clean.csv")` → `((t<0)|(t>40)).sum()`, `t.min()`, `t.max()` | `Temp min/max 23.0000/27.7500 hors[0,40]=0` — 0 valeur hors bornes, 0 manquante. Le `-127.0` du brut (2021-08-30 07:02:26) est marqué `Temperature_imputed=True` et remplacé par `24.5` (interpolation sur un trou de 13 min 34 s) | ✅ |
| 2 | Aucune valeur de pH hors [0, 14] (idéalement resserré à une plage réaliste) | Idem, `((p<0)|(p>14)).sum()`, `p.min()`, `p.max()`, puis comptage `p<4` | `PH min/max 1.14327/8.55167 hors[0,14]=0` — les 40 pH négatifs du brut sont marqués et laissés manquants (trous > 1 h). Le resserrement « idéalement réaliste » est **explicitement différé au Jalon 2 par l'ADR-010 accepté** ; il reste 145 relevés < 4 dans le livrable (145 + 40 négatifs = 185, chiffre annoncé par l'ADR-010, confirmé) | ✅ (partie obligatoire) |
| 3 | Ammoniac : outliers extrêmes traités et justifiés (exclusion documentée ou transformation) | Traitement : `(am>5).sum()` sur le livrable ; justification : recalcul de la distribution du sous-ensemble `>5` du brut (`nunique`, quantiles, répartition par décade) confrontée au texte d'ADR-003 | **Traitement conforme** : `NH3 min/max 0.006770/4.981840 >5=0`, 27 560 valeurs marquées, 4 337 imputées, 23 275 laissées manquantes, note ADR-003 présente dans le rapport. **Justification inexacte** : ADR-003 affirme que ce sous-ensemble « est en réalité un plateau de valeurs strictement identiques à 4,27 × 10¹¹ … non une distribution continue ». Mesuré : `ammoniac >5 : n=27560 | valeurs uniques=1838 | min=5.00082 max=4.27e+11`, médiane 127,87, q25 15,78, q75 10 848 ; **25 relevés seulement** sont exactement au maximum ; 3 847 (13,96 %) sont entre 5 et 10 ; 369 (1,34 %) seulement ≥ 10⁹. Le tableau §4 du rapport d'analyse dit d'ailleurs la même chose (> 5 : 27 560 mais > 10⁹ : 369) : c'est la phrase de synthèse qui est fausse, pas le chiffrage | ⚠️ |
| 4 | Courbe de croissance monotone ou quasi-monotone après reconstruction des paliers, non-monotonies signalées et non corrigées | `ingestion.rebuild_growth_curve(load_raw_data("data/raw/IoTpond1.csv"))` puis analyse des différences successives | 81 paliers (contre 83 126 lignes brutes) ; `Fish_Weight(g)` : **0 baisse** sur 80 transitions ; `Fish_Length(cm)` : **3 baisses (3,75 %)** aux 2021-07-15 (−2,77), 2021-08-22 (−0,53), 2021-09-05 (−13,76), toutes portées par `Fish_Length_non_monotonic=True` **et** un `logging.warning` daté, valeurs brutes conservées telles quelles (15.31, 23.4, 12.09) — aucune correction silencieuse | ✅ |
| 5 | Le rapport de nettoyage indique le nombre de valeurs corrigées par colonne | Lecture de `reports/cleaning_report.json` + recomptage indépendant sur le brut + recalcul du SHA-256 | Rapport complet pour les 6 colonnes de qualité d'eau : `bounds`, `n_out_of_bounds`, `n_missing_raw`, `n_imputed`, `n_still_missing`, type de capteur, applicabilité des seuils. **Tous les comptages reproduits à l'identique** (Temp 1/1/0 · PH 40/0/40 · DO 21 614/8 371/13 243 · NH3 27 560 + 52 NaN/4 337/23 275 · Nitrate et Turbidity : aucune borne). Bornes appliquées présentes. Empreinte du brut présente et exacte : `sha256 raw = 063ea4f0c9fcd24f016fbfc52e5422b655dfb4f3542d40476d99d899655dda76` = valeur du rapport = valeur de `docs/01`. Réserve mineure : 5 des 11 colonnes (`created_at`, `entry_id`, `Population`, `Fish_Length`, `Fish_Weight`) n'apparaissent pas du tout dans le rapport, alors que `Fish_Length`/`Fish_Weight` portent 2 valeurs manquantes chacune, et les résultats de `rebuild_growth_curve` ne sont persistés dans aucun livrable | ✅ |

**Definition of Done `docs/02` §8, point 1 (« plus de valeurs physiquement impossibles ») :** vérifié au-delà des quatre bornes — aucune valeur négative dans aucune colonne numérique du livrable, `Population` constante à 50, `entry_id` sans doublon, température toujours dans [20, 35] (plage critique du cahier §5), turbidité 1 → 100, nitrate 45 → 1 936 (sans borne, ADR-009). ✅

---

## 2. Contrôles spécifiques demandés au brief

| # | Contrôle | Méthode / commande | Observé | Verdict |
|---|---|---|---|---|
| 6 | Conformité aux ADR-003/009/010 : bornes exactes, aucune valeur inventée, nitrate sans borne absolue, structure « type de capteur » réellement protectrice | Lecture de `src/config.py` L99-189 confrontée mot à mot aux ADR ; `tests/test_config.py` (18 tests) ; recherche d'un couplage `THRESHOLDS` ↔ `SENSOR_TYPES` dans `src/` | Bornes **exactement** conformes : `TEMPERATURE_BOUNDS {0,40}`, `PH_BOUNDS {0,14}`, `AMMONIA_BOUNDS {"max":5}` (ADR-003), `DISSOLVED_OXYGEN_BOUNDS {"max":15}` (ADR-010, plus aucune mention « provisoire »), `NITRATE_BOUNDS = None` (ADR-009), `MAX_INTERPOLATION_GAP = "1h"`, `RESAMPLING_FREQUENCY = None` (application différée au Jalon 2, conforme à l'ADR-010). Aucune valeur non couverte par un ADR. `SENSOR_TYPES` **déclare** correctement `gas` / `absolute_thresholds_applicable=False` pour ammoniac et nitrate — mais ne **protège pas** : `THRESHOLDS["ammonia"] = {"max":0.05,"critical_max":0.1}` et `THRESHOLDS["nitrate"] = {"max":50,"critical_max":100}` restent importables tels quels, aucun code ne croise les deux tables, et le commentaire de `THRESHOLDS["nitrate"]` (L91) invoque toujours « unité non tranchée — décision A1 », décision close par l'ADR-009 | ⚠️ |
| 7 | Nettoyage non destructif : 83 126 lignes, colonne de marquage par variable bornée, aucune imputation au-delà de 1 h, aucune extrapolation en bord de série | Comparaison ligne à ligne brut ↔ livrable ; recalcul indépendant du masque « hors bornes ou manquant », du trou encadrant (`ffill`/`bfill` des horodatages valides) et des positions de bord | `n_rows = 83126` (identique au brut, `entry_id` et horodatages alignés un à un). 4 colonnes de marquage, une par variable bornée (`Temperature_imputed`, `PH_imputed`, `Dissolved Oxygen_imputed`, `Ammonia_imputed`), dtype `bool`, **strictement égales au masque recalculé** ; aucune valeur manquante non marquée. **Aucune valeur valide modifiée** (83 125/83 125 pour la température, 83 086/83 086 pH, 61 512/61 512 DO, 55 514/55 514 NH3). **0 imputation au-delà de 1 h** (trou maximal effectivement comblé : 54 min 09 s pour le DO, 59 min 12 s pour l'ammoniac, 13 min 34 s pour la température) ; **0 imputation en bord de série** (avant le premier ou après le dernier point valide) | ✅ |
| 8 | Ampleur des données manquantes après nettoyage, par colonne, et conséquence pour les Jalons 2 et 3 | Comptage des `NaN` par colonne du livrable ; répartition par jour, plus longue série contiguë ; couverture des créneaux horaires (fréquence du Jalon 2, ADR-010) ; part de valeurs reconstituées parmi les valeurs présentes | Voir §4 ci-dessous. Ammoniac **28,00 %** manquant (23 275), oxygène dissous **15,93 %** (13 243), pH 0,05 % (40), autres colonnes 0 %. **36,96 % des lignes** ont au moins une des quatre variables bornées manquante. Trous concentrés (NH3 : 9 jours entiers sans aucune valeur, plus longue série 2 j 13 h ; DO : 2 jours entiers, plus longue série 1 j 12 h). Ce n'est pas un défaut du nettoyage — c'est la conséquence arithmétique des bornes tranchées en G1 — mais c'est un point de vigilance réel et chiffré pour les Jalons 2 et 3 | ⚠️ (vigilance, non bloquant) |
| 9 | Horodatage : suffixe retiré, aucune conversion, ordre chronologique, doublons comptés | Recalcul sur le livrable et comparaison valeur par valeur avec le brut après simple retrait du suffixe | `contient CET: False` ; `ts min/max: 2021-06-19 00:00:05 | 2021-10-13 04:14:22` — identiques au brut privé de « CET », **aucun décalage horaire appliqué** (comparaison stricte des 83 126 horodatages : égalité totale) ; `monotonic: True` ; `doublons: 0` (valeur du rapport : `n_duplicate_timestamps: 0`) ; `NaT: 0` | ✅ |

---

## 3. Contrôles transverses

**Tests.** `venv/Scripts/python.exe -m pytest -q` → `59 passed, 31 skipped in 7.66s`, aucun échec. Les 31 `skip` sont tous rattachés aux Jalons 2 à 6 (motifs explicites). ✅

**Qualité des tests du Jalon 1.** 21 tests réels dans `tests/test_ingestion.py` et 18 dans `tests/test_config.py` (aucun `skip` résiduel sur le périmètre du jalon), sur jeux synthétiques. Les cas limites **exactement à la borne** sont couverts et discriminants : température `0.0` et `40.0` conservées vs `40.0001` marquée ; pH `0.0`/`14.0` vs `14.0001` ; ammoniac `5.0` conservé vs `5.0001` marqué ; DO `15.0` conservé vs `15.0001` marqué ; trou de **exactement 1 h** comblé (valeur attendue vérifiée : `pytest.approx(11.0)`) vs trou de 1 h 30 non comblé ; valeur hors bornes en fin de série laissée `NaN` (non-extrapolation) ; `-127 °C` et pH négatif traités nommément ; nitrate sans colonne de marquage (ADR-009) ; empreinte absente → `None` et non devinée. Tests non triviaux, assertions portant sur les valeurs et non seulement sur la forme. ✅

**Traçabilité.** `venv/Scripts/python.exe .claude/hooks/audit_trace.py --date 2026-09-18` → `Verdict : ✅ CONFORME` — 25 entrées, 243 actions automatiques, `Fichiers modifiés non cités dans le journal (0)`, `Entrées mal formées (0)`. Contrôle de la veille (dernière validation antérieure) : `--date 2026-09-17` → `✅ CONFORME`. Les 5 échecs/refus enregistrés le 2026-09-18 sont documentés (dont un refus de périmètre `Write` du qa-validator lui-même, sur le dossier scratchpad — contourné en exécutant les vérifications en mémoire, sans écrire de fichier). Les entrées J-20260918-019, 023 et 024 décrivent l'implémentation et ses deux corrections successives, avec preuves. ✅

**Valeurs en dur.** Balayage AST de tous les fichiers de `src/` (hors `config.py`) et `dashboard/`, hors docstrings : **aucun seuil numérique ni chemin littéral**. Deux points mineurs subsistent dans `src/ingestion.py` : les notes par colonne sont attachées par comparaison à des noms de colonnes littéraux (`'Ammonia(g/ml)'` L247, `'Dissolved Oxygen(g/ml)'` L253) et le texte de la note DO duplique la valeur de la borne en clair (« borne haute définitive 15 mg/L … candidates 8 / 15 / 20 mg/L », L255) — si `DISSOLVED_OXYGEN_BOUNDS` changeait, le rapport continuerait d'annoncer 15. ⚠️ mineur

**Cohérence `src/config.py` ↔ `docs/` ↔ ADR.** Bornes, limite d'interpolation, suffixe d'horodatage et empreinte du brut : **cohérents** entre `config.py`, `docs/01` (anomalies 1, 2, 3, 7 à 11, toutes marquées tranchées) et les ADR-003/009/010. Quatre écarts documentaires subsistent : `docs/03-ARCHITECTURE_CODE.md` (§`src/config.py`) décrit encore `AMMONIA_BOUNDS`, `DISSOLVED_OXYGEN_BOUNDS`, `NITRATE_BOUNDS`, `MAX_INTERPOLATION_GAP` comme « décisions ouvertes, à `None` » et `TIMESTAMP_SUFFIX` comme « fuseau horaire = décision ouverte » ; `docs/11-TABLEAU_DE_BORD.md` §Situation affirme « seule la borne haute de l'oxygène dissous reste ouverte (A2) » alors que la même page conclut plus bas « plus aucune décision en attente pour le Jalon 1 » ; la docstring de `src/features.py::add_threshold_distance` fonde encore l'exclusion du DO, de l'ammoniac et du nitrate sur la décision A1 « non tranchée » et sur des bornes « encore `None` » ; le commentaire de `THRESHOLDS["nitrate"]` dans `config.py` invoque la même décision close. ⚠️

**Périmètre.** Le travail livré (chargement, validation de schéma, bornes, imputation limitée, reconstruction de la courbe de croissance, rapport) relève strictement de « Nettoyage, exploration et structuration des données » (`cahier des charges` §3, dans le périmètre). Aucune fonctionnalité hors périmètre introduite : `features.py`, `decision_engine.py`, `main.py`, `dashboard/app.py` restent des squelettes levant `NotImplementedError`. ✅

**Git.** `git status` : 6 fichiers modifiés (`src/config.py`, `src/ingestion.py`, `tests/test_config.py`, `tests/test_ingestion.py`, `logs/agents/actions.jsonl`, `logs/agents/journal/2026-09-18.md`) et un fichier non suivi (`reports/cleaning_report.json`) — tous cohérents avec le Jalon 1 ; le travail du data-engineer n'est pas encore commité (c'est l'orchestrateur qui commite, `.claude/rules/git-workflow.md` §1). `git check-ignore -v` confirme que `data/raw/IoTpond1.csv` (règle `.gitignore:11`) et `data/processed/pond1_clean.csv` (règle `.gitignore:15`) **restent ignorés** ; aucun fichier interdit n'est en attente de commit. `reports/cleaning_report.json` (2,9 ko) n'est pas ignoré et **doit** rejoindre le commit du jalon : c'est le livrable versionnable. Les 5 commits de `git log origin/main..HEAD` respectent les Conventional Commits et portent tous les trailers `Agent`, `Jalon`, `Journal` (et `Refs` quand un ADR est concerné). ✅

**Reproductibilité du livrable (contrôle ajouté par le validateur).** Régénération complète en mémoire par `load_raw_data` + `clean_data` sur `data/raw/IoTpond1.csv`, puis comparaison colonne par colonne avec le fichier sur disque : **0 colonne différente** (14 colonnes, 83 126 lignes, égalité numérique à 1e-9 et égalité stricte des drapeaux et horodatages), et rapport régénéré identique au JSON livré sur toutes ses clés. Le livrable correspond donc exactement au code présent. En revanche, **aucun script, CLI ou notebook du dépôt n'écrit ces deux chemins** : `PROCESSED_DATA_PATH` et `CLEANING_REPORT_PATH` ne sont référencés que par `tests/test_config.py` et des docstrings ; `src/main.py` lève `NotImplementedError` (Jalon 6) et `notebooks/` est vide. Le livrable a été produit par une exécution ad hoc, non rejouable par un tiers en une commande.

---

## 4. Données manquantes après nettoyage — chiffrage et conséquence pour les Jalons 2 et 3

| Colonne | Manquantes | % du fichier | Concentration temporelle | Valeurs reconstituées parmi les valeurs présentes |
|---|---:|---:|---|---:|
| `Temperature (C)` | 0 | 0,00 % | — | 1 (0,00 %) |
| `Turbidity(NTU)` | 0 | 0,00 % | — | 0 (aucune borne) |
| `Nitrate(g/ml)` | 0 | 0,00 % | — | 0 (aucune borne, ADR-009) |
| `PH` | 40 | 0,05 % | 8 jours à 100 % manquant ; plus longue série 40 relevés étalée sur 7 j 03 h (24/09 → 01/10, période très clairsemée) | 0 |
| `Dissolved Oxygen(g/ml)` | 13 243 | **15,93 %** | 6 jours > 50 % manquant, 2 jours à 100 % ; plus longue série 4 729 relevés = 1 j 12 h 51 (30/07 → 31/07) | 8 371 sur 69 883 présentes (**11,98 %**) |
| `Ammonia(g/ml)` | 23 275 | **28,00 %** | 26 jours > 50 % manquant, 9 jours à 100 % ; plus longue série 2 994 relevés = 2 j 13 h 08 (13/07 → 16/07) | 4 337 sur 59 851 présentes (**7,25 %**) |

- **Lignes complètes** (les 4 variables bornées non manquantes) : 52 399, soit **63,04 %** ; 36,96 % des lignes ont au moins une lacune.
- **Effet sur le ré-échantillonnage horaire du Jalon 2** (fréquence tranchée par l'ADR-010) : sur les 2 789 créneaux horaires de l'étendue, 1 431 (51,31 %) contiennent au moins un relevé. Après nettoyage, la part de créneaux horaires portant **au moins une valeur exploitable** tombe à 1 301 pour le DO (46,65 % de l'étendue, 90,92 % des créneaux non vides) et à **980 pour l'ammoniac (35,14 % de l'étendue, 68,48 % des créneaux non vides)** ; température, nitrate et turbidité restent à 100 % des créneaux non vides.

**Est-ce que cela met en cause les Jalons 2 et 3 ?** Non pas au sens d'un blocage, mais au sens d'un rétrécissement réel du jeu exploitable, à documenter et à arbitrer avant le Jalon 3 :

1. **Jalon 2** (exploration, corrélations qualité d'eau ↔ croissance) : reste faisable ; les corrélations impliquant l'ammoniac porteront sur environ un tiers des créneaux horaires de l'étendue et sur des périodes discontinues — toute corrélation calculée sur l'ammoniac devra indiquer son effectif et sa couverture temporelle, sinon elle sera ininterprétable.
2. **Jalon 3** (modèle de risque) : le risque n'est pas le volume (52 399 lignes complètes suffisent) mais **le biais de sélection** — les lacunes ne sont pas aléatoires, elles coïncident avec les régimes de capteur anormaux (plateau DO du 30/07, épisode ammoniac du 13-16/07). Un modèle entraîné sur les seules lignes complètes apprendrait un cycle expurgé de ses épisodes les plus atypiques, c'est-à-dire précisément ceux que la démonstration veut détecter. Par ailleurs, 11,98 % des valeurs de DO présentes et 7,25 % de celles d'ammoniac sont des valeurs **interpolées, pas mesurées** : elles ne doivent pas être traitées comme des observations. Les drapeaux `<label>_imputed` permettent de les isoler, à condition que le Jalon 3 les consulte explicitement.
3. **Point d'attention de nommage** : le drapeau `<label>_imputed` vaut `True` aussi bien pour une valeur reconstituée que pour une valeur restée manquante (13 243 cas pour le DO, 23 275 pour l'ammoniac). Son nom suggère l'inverse ; seul le test `notna()` distingue les deux. `docs/03` (§`decision_engine.py`) parle déjà de « valeur absente ou imputée-manquante », donc le cas est anticipé, mais le schéma du fichier nettoyé n'est documenté nulle part.

---

## 5. Défauts à corriger

| # | Défaut | Preuve | Agent propriétaire | Bloquant ? |
|---|---|---|---|---|
| D1 | ADR-003 et `reports/analyse-donnees-jalon1.md` (§A ligne 16, §4 ligne 152) décrivent les 27 560 relevés d'ammoniac > 5 comme « un plateau de valeurs strictement identiques à 4,27 × 10¹¹ … pas une distribution continue ». C'est faux : 1 838 valeurs distinctes, de 5,00082 à 4,27e11, médiane 127,87 ; **25 relevés seulement** au maximum exact ; 13,96 % des valeurs > 5 sont entre 5 et 10. Le tableau de répartition du même §4 (> 5 : 27 560 · > 10⁹ : 369) contredit déjà la phrase | Recalcul validateur sur `data/raw/IoTpond1.csv` : `valeurs uniques=1838 | min=5.00082 max=4.27e+11`, quantiles `{0.25: 15.78, 0.5: 127.87, 0.75: 10848.02}` | doc-keeper (texte d'ADR-003) et data-engineer (phrase de synthèse du rapport d'analyse) — **décision de fond : humain G1** | Non bloquant pour le livrable ; **bloquant pour le mémoire** et à retrancher par l'humain |
| D2 | `SENSOR_TYPES` déclare mais n'empêche pas : `THRESHOLDS["ammonia"]` et `THRESHOLDS["nitrate"]` restent des seuils absolus importables, sans lien de code avec `absolute_thresholds_applicable`. Le commentaire de `THRESHOLDS["nitrate"]` (`src/config.py` L91) invoque encore « unité non tranchée — décision A1 », close par l'ADR-009 | Lecture `src/config.py` L86-93 vs L152-189 ; aucun croisement des deux tables dans `src/` (recherche sur tout `src/`) | data-engineer (commentaire et éventuel garde-fou), orchestrateur si un ADR est nécessaire (table partagée) | Non bloquant au Jalon 1 ; à traiter **avant** les Jalons 3 et 4 |
| D3 | La docstring de `src/features.py::add_threshold_distance` fonde l'exclusion de `dissolved_oxygen`, `ammonia` et `nitrate` sur « l'unité pas tranchée (décision A1) » et sur des bornes « encore `None` » — trois affirmations périmées depuis ADR-009/010 (le DO a désormais des seuils applicables, l'ammoniac et le nitrate sont exclus pour une autre raison : capteur de gaz) | `src/features.py` L77-86 ; déjà signalé par le data-engineer lui-même dans J-20260918-019 | data-engineer | Non bloquant (Jalon 2), à corriger avant d'implémenter `features.py` |
| D4 | `docs/03-ARCHITECTURE_CODE.md` (§`src/config.py`) décrit toujours `AMMONIA_BOUNDS`, `DISSOLVED_OXYGEN_BOUNDS`, `NITRATE_BOUNDS`, `MAX_INTERPOLATION_GAP` comme « décisions ouvertes à `None` » et `TIMESTAMP_SUFFIX` comme « fuseau horaire = décision ouverte » | `docs/03` §`src/config.py`, puces « Schéma brut attendu » et « Nettoyage » | doc-keeper | Non bloquant |
| D5 | `docs/11-TABLEAU_DE_BORD.md` se contredit : §Situation « seule la borne haute de l'oxygène dissous reste ouverte (A2) » vs §Décisions en attente « plus aucune décision en attente pour le Jalon 1 » | `docs/11` L10 vs L42 | doc-keeper | Non bloquant |
| D6 | Le rapport de nettoyage ne couvre que 6 des 11 colonnes ; `Fish_Length(cm)` et `Fish_Weight(g)` (2 valeurs manquantes chacune, documentées dans `docs/01`) n'y figurent pas, et les résultats de `rebuild_growth_curve` (81 paliers, 3 non-monotonies datées) ne sont persistés dans aucun livrable — ils n'existent que dans un `logging.warning` d'exécution et dans le journal J-20260918-019 | `reports/cleaning_report.json` (clé `columns`) ; `src/ingestion.py` L324-336 | data-engineer | Non bloquant ; affaiblit la preuve du critère 4 dans la durée |
| D7 | Aucun point d'entrée ne régénère `data/processed/pond1_clean.csv` ni `reports/cleaning_report.json` : aucun script, CLI ou notebook n'écrit `PROCESSED_DATA_PATH`/`CLEANING_REPORT_PATH` ; `src/main.py` lève `NotImplementedError` (Jalon 6) et `notebooks/` est vide. Le livrable, ignoré par git, n'est donc pas régénérable par un tiers en une commande — alors que le README annonce `python src\main.py # pipeline complet` | Recherche sur tout le dépôt des deux constantes ; `src/main.py::run_pipeline` ; `README.md` §Utilisation. Reproductibilité tout de même **prouvée** par le validateur via un appel direct des deux fonctions (0 différence) | À trancher : data-engineer (maintenant) ou automation-engineer (Jalon 6) | Non bloquant au Jalon 1 ; **bloquant pour `docs/02` §8 et le Jalon 6** |
| D8 | `src/ingestion.py` attache ses notes par comparaison à des noms de colonnes littéraux (L247, L253) et la note DO répète la valeur de la borne en texte (« 15 mg/L … 8 / 15 / 20 », L255) : si `config.DISSOLVED_OXYGEN_BOUNDS` ou `RAW_COLUMNS` changeait, le rapport mentirait ou perdrait sa note sans qu'aucun test n'échoue | `src/ingestion.py` L247-258 ; balayage AST des littéraux | data-engineer | Non bloquant (mineur) |
| D9 | Le schéma du fichier nettoyé (15 colonnes, dont 4 drapeaux `<label>_imputed`) n'est documenté nulle part, et la sémantique du drapeau est contre-intuitive : `True` couvre aussi bien « valeur reconstituée » que « valeur restée manquante » (13 243 DO, 23 275 NH3) | `data/processed/pond1_clean.csv` (en-tête) ; `docs/01` ne décrit que le fichier brut | data-engineer (+ doc-keeper pour `docs/01`) | Non bloquant ; à documenter avant les Jalons 3 et 4 |

Aucun défaut bloquant pour les cinq critères de `docs/04`.

---

## 6. Points à trancher par l'humain

1. **G1 — Seuil ammoniac (ADR-003), à reconfirmer en connaissance de cause.** La décision « seuillé à 5 » a été prise sur la base d'une description du sous-ensemble > 5 comme « plateau constant à 4,27e11, de même nature que le code d'erreur −127 ». Les données montrent un **continuum** : 1 838 valeurs distinctes, dont 3 847 relevés entre 5 et 10 et une médiane à 127,87. Le seuil peut rester le bon choix, mais il ne peut plus s'appuyer sur cet argument : il faut soit le reformuler (seuil de plausibilité aquacole plutôt que détection d'un code d'erreur), soit reconsidérer la valeur. Le validateur ne propose pas de cible ; l'arbitrage appartient à l'humain, et l'ADR-003 devra être corrigé quoi qu'il arrive (règle : un ADR accepté n'est pas réécrit, la correction passe par une révision datée ou un nouvel ADR).
2. **G1 — Acceptation du périmètre de données restant.** 28,00 % de l'ammoniac et 15,93 % de l'oxygène dissous sont manquants après nettoyage, avec des trous concentrés sur les épisodes les plus atypiques du cycle ; seuls 35,14 % des créneaux horaires de l'étendue porteront une valeur d'ammoniac exploitable au Jalon 2. Confirmer qu'on avance sur cette base (et que les Jalons 2-3 documenteront effectifs et couverture), ou réviser une borne.
3. **G1 — pH.** L'ADR-010 a différé au Jalon 2 le resserrement à une plage réaliste ; 145 relevés < 4 subsistent dans le livrable. Confirmer le report, ou trancher maintenant.
4. **G1 — Régénération du livrable (D7).** Décider qui fournit le point d'entrée et quand : une commande dès le Jalon 1 (data-engineer) ou au Jalon 6 avec le pipeline complet (automation-engineer). En l'état, le README promet une commande qui échoue.
5. **G2 — Verdict final du Jalon 1.** Le validateur propose **VALIDÉ AVEC RÉSERVES** : les cinq critères de `docs/04` sont tenus et vérifiés par recalcul indépendant ; les réserves portent sur la justification écrite d'ADR-003 (D1), sur la régénération du livrable (D7) et sur les écarts documentaires D2 à D5, tous hors du périmètre strict des critères.

---

*Rapport produit sans modifier aucun fichier du projet hors `reports/validations/` et `logs/agents/journal/`. Toutes les mesures ci-dessus sont reproductibles : lecture directe de `data/processed/pond1_clean.csv` et `data/raw/IoTpond1.csv` avec pandas, appels de `src.ingestion`, `pytest -q`, `audit_trace.py`, commandes git en lecture seule.*
