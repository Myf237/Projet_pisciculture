# Validation — Préparation : mise en place du projet
- Date : 2026-09-17 11:41 · Itération : 1 · Validateur : qa-validator
- Périmètre : branche `chore/mise-en-place-projet` (rien de commité), travaux des 16 et 17/09 (J-20260916-001 à 027, J-20260917-001 à 004). Hors jalon `docs/04` : critères du brief de l'orchestrateur (J-20260917-004).
- Verdict proposé : **NON VALIDÉ** — 4 défauts bloquants avant fusion, tous corrigeables rapidement (entrée de journal correctrice, explication d'une écriture non tracée, suppression d'une valeur de repli, mise à jour de `docs/11`). La PR peut être ouverte en brouillon en attendant. Décision finale : humain (G2).

Environnement : `venv/Scripts/python.exe` (Python 3.12), commandes lancées depuis la racine du projet. Les scripts d'analyse en lecture seule (`check_signatures.py`, `check_skeleton.py`, `literals.py`, `check_config_docs.py`, `check_settings.py`, `check_tests.py`, `check_prefixes.py`) sont dans le scratchpad de session, hors dépôt ; leur méthode est décrite ci-dessous pour pouvoir les rejouer.

| # | Critère (brief de préparation) | Méthode / commande | Observé | Verdict |
|---|---|---|---|---|
| 1 | Structure conforme à `docs/03` ; dossiers des fichiers futurs présents | `find . -path ./.git -prune -o -path ./venv -prune …` + test d'existence de chaque entrée de l'arborescence de `docs/03` | Tous les fichiers prévus maintenant existent (`src/` 8 fichiers, `tests/` 7, `dashboard/app.py`, `data/README.md`, `.github/pull_request_template.md`, `LICENSE`, `.gitattributes`, `pytest.ini`, `README.md`, `requirements.txt`). Dossiers `data/processed/`, `models/`, `notebooks/`, `reports/figures/`, `logs/agents/` présents. **`data/raw/` ABSENT** : création refusée aux agents par permission (J-20260916-019), à faire par l'humain (`data/README.md`) ; pas de `data/raw/.gitkeep`, alors que `.gitignore` le prévoit (`!data/raw/.gitkeep`). Un clone n'aura donc pas ce dossier. `reports/validations/` était absent au début de la vérification ; ce rapport le crée. | ⚠️ |
| 2 | Signatures publiques de `src/` identiques à `docs/03` | Analyse `ast` des blocs ```python de `docs/03` comparée à l'`ast` du code **et** à `inspect.signature` (noms, annotations, défauts, retour), puis recherche de fonctions publiques non documentées | 19 fonctions documentées : **18 identiques**, **1 équivalente en valeur** (`add_rolling_features(df, window: str = config.ROLLING_WINDOW_DEFAULT)`, défaut évalué `'1h'` = `docs/03`). 0 absente, 0 non documentée. Ex. `log_decision(action: 'dict', log_path: 'str' = 'logs/decisions.log') -> 'None'`, `main(argv: 'list[str] \| None' = None) -> 'int'`. | ✅ |
| 3 | `pytest -q` vert ; chaque `skip` cite un jalon et un critère ou une règle ; aucun test actif vide ou trivial | `venv/Scripts/python.exe -m pytest -q` ; `ast` des tests (décorateurs `skip`, nombre d'`assert` et de `pytest.raises`, assertions constantes) | `29 passed, 38 skipped in 4.14s`, 0 échec. Les 38 motifs de `skip` citent tous un « Jalon N » et une règle, un critère, une section `docs/` ou une décision (A3, A4, A5) ; leur corps est vide, ce qui est normal pour un test ignoré. 29 tests actifs, chacun avec au moins 1 `assert` ou `pytest.raises`, et aucun `assert` constant. `git status` inchangé après exécution (67 entrées). | ✅ |
| 4 | Squelette sans logique métier ; aucune décision ouverte tranchée ; aucune valeur inventée dans `config.py` | `ast` des corps de fonction, puis appel réel de chaque fonction métier ; `python src/main.py` ; `python -m src.decision_engine` ; lecture de `src/config.py` et recherche de mots-clés (mg/L, UTC, CEST, tz_localize, hystérésis, IsolationForest, resample…) | Les 16 fonctions métier ont pour seul corps `raise NotImplementedError("Jalon N — … (docs/0X …)")`. `run_pipeline` journalise les 4 étapes prévues puis lève `NotImplementedError`. CLI : code 1 avec un message clair et sans trace d'exception (« Erreur : pipeline non implémenté (Jalon 6) … », « Erreur : moteur de décision non implémenté (Jalon 4) … »). Config : `AMMONIA_BOUNDS`, `DISSOLVED_OXYGEN_BOUNDS`, `NITRATE_BOUNDS`, `MAX_INTERPOLATION_GAP`, `RESAMPLING_FREQUENCY` = `None`, avec leur référence. Turbidité = `None`. A3, A4, A5 et anti-oscillation sont signalés comme ouverts. Le retrait du suffixe « CET » est exigé par `docs/02` §2 et ne tranche pas le fuseau. Toutes les valeurs de la config sont sourcées (cahier §5, `docs/01`, `docs/02` §4.1-4.2), sauf `RANDOM_STATE = 42`, une graine arbitraire sans portée scientifique (voir observations). | ✅ |
| 5 | Aucune valeur en dur hors `src/config.py` (`src/`, `dashboard/`, `tests/`), dont les valeurs de repli qui dupliquent la configuration | `ast` : littéraux numériques et chaînes de type chemin, hors docstrings, dans `src/`, `dashboard/` et `tests/` (sauf `config.py`) | **`src/main.py:36` `_FALLBACK_RAW_DATA_PATH = "data/raw/IoTpond1.csv"`** : cette valeur de repli duplique `config.RAW_DATA_PATH`. Le chemin est relatif au répertoire courant, contrairement à `conventions-code.md` (« construits à partir de la racine … jamais relatifs au répertoire courant »). La branche est morte puisque `config.py` existe. `src/decision_engine.py:81` : le défaut littéral `log_path = "logs/decisions.log"` est imposé par le contrat de `docs/03`, mais `config.DECISIONS_LOG_PATH` existe : c'est un conflit entre sources (D6). Les autres littéraux sont légitimes : oracles de `tests/test_config.py` (26, 32, 0.05…, rôle du test), codes de sortie, `timeout=30` et libellé d'IHM `ACTUATOR_BANNER`. | ❌ |
| 6 | `THRESHOLDS` et bornes identiques à `docs/03`, `docs/01` (anomalies 1-2) et au cahier §5 | `ast.literal_eval` du bloc `THRESHOLDS` de `docs/03`, comparé à `config.THRESHOLDS` ; bornes extraites de `docs/03` ; tableau §5 du cahier analysé et comparé | `THRESHOLDS docs/03 == config : True`, et l'extrait est identique au code caractère pour caractère. Cahier §5 : température 26–32 / 20–35, OD 4 / 3, pH 6.5–8.5 / 6–9, ammoniac 0.05 / 0.1, nitrate 50 / 100, turbidité « à définir » → tout est identique (turbidité = `None`). `TEMPERATURE_BOUNDS {'min': 0, 'max': 40}` et `PH_BOUNDS {'min': 0, 'max': 14}` sont identiques à `docs/01` anomalies 1-2 et à `docs/04`. Aucun ADR accepté ne porte sur un seuil. | ✅ |
| 7 | Traçabilité : audits du 16 et du 17 conformes ; chaque agent a son entrée ; corrections par nouvelles entrées (016, 026) ; aucune réécriture | `python .claude/hooks/audit_trace.py --date 2026-09-16` et `--date 2026-09-17` ; croisement des horodatages de `actions.jsonl` avec les en-têtes d'entrée ; numérotation et ordre ; comparaison du début de chaque entrée ajoutée par shell avec le texte tracé ; `stat` du journal | Audits : **16/09 ✅ CONFORME** (27 entrées, 221 actions, 0 fichier non cité, 0 entrée mal formée) ; **17/09 ✅ CONFORME** (4 entrées, 18 actions au moment de l'audit). Tous les agents ayant agi ont une entrée. Corrections : J-20260916-016 (attribution) et J-20260916-026 (heures de 014 à 018 et de 024) existent. **Mais J-20260916-023 (en-tête 21:15) a été écrite à 20:43:37 et J-20260916-025 (en-tête 21:24) à 20:48:33** (`actions.jsonl` l. 242 et 274 ; aucune action du doc-keeper après 20:48:41). Ces deux heures ne sont pas corrigées, et **J-20260916-026 affirme à tort que « les heures des entrées 019 à 023 … sont exactes »**. Réécriture : numéros 001 à 027 continus et uniques ; ordre du fichier identique à l'ordre d'ajout dans `actions.jsonl` (022, 020, 024, 021, 023, 019, 025, 026, 027) ; début des 12 entrées ajoutées par shell identique au texte tracé. **Cependant, `2026-09-16.md` a été modifié pour la dernière fois à 21:11:14, alors que la dernière écriture tracée date de 20:56:27** : cette modification n'est pas expliquée, et le contenu des entrées écrites par Edit ou par script (001 à 013, 023, 025, 026) ne peut pas être vérifié. | ❌ |
| 8 | Garde-fous : JSON valide, scripts de hooks existants, protection de `data/raw/` et d'`actions.jsonl`, garde-fou de périmètre du qa-validator et du doc-keeper | `json.loads` de `.claude/settings.json` ; résolution de `${CLAUDE_PROJECT_DIR}` et test d'existence ; lecture de l'en-tête de chaque agent. **Aucun garde-fou exécuté volontairement.** | JSON valide (`$schema`, `permissions`, `hooks`). 8 déclarations de hooks (`PreToolUse` → `guard_git.py` ; `SessionStart`, `SubagentStart/Stop`, `PostToolUse`, `PostToolUseFailure`, `PermissionDenied` → `log_action.py` ; `SubagentStop` → `check_journal.py`) : tous les scripts existent. `permissions.deny = ['Edit(/data/raw/**)', 'Edit(/logs/agents/actions.jsonl)']`, refus effectif constaté le 16/09 (J-20260916-011). En-têtes : `qa-validator` → `guard_paths.py reports/validations/ logs/agents/journal/` ; `doc-keeper` → `guard_paths.py docs/ README.md logs/agents/journal/`. Constat involontaire : ma commande de lecture `git branch --show-current` a été refusée à 11:24:21 et tracée `refuse` dans `actions.jsonl`. Ce n'était pas un test : `guard_git.py` bloque bien les sous-agents, mais produit un faux positif (D8). | ✅ |
| 9 | Prêt à commiter | `git status -b --short --untracked-files=all` ; `git check-ignore -v venv data/raw` ; `git check-ignore -v ./.claude/hooks/__pycache__/audit_trace.cpython-312.pyc` ; tailles (`stat -c %s`) ; recherche de secrets (motifs clé, jeton, `ghp_`, `gho_`, clé privée…) ; `git ls-files --eol -o --exclude-standard` ; `git rev-parse --abbrev-ref HEAD` ; `git log --oneline origin/main..HEAD` | Branche `chore/mise-en-place-projet` (≠ `main`), `HEAD = origin/main = 00cfe47`. `git log origin/main..HEAD` est vide. 67 fichiers non suivis, tous attendus. `venv` ignoré (`.gitignore:2`), `__pycache__` ignoré (`.gitignore:4`), `.pytest_cache` ignoré. `data/raw` : le dossier est absent et ne correspond à aucun motif (code 1), mais son contenu est ignoré (`.gitignore:11:data/raw/*  data/raw/IoTpond1.csv`, code 0). Aucun fichier de `data/raw/`, de `venv/` ni aucun artefact régénérable ; fichier le plus gros : `logs/agents/actions.jsonl`, environ 104 Ko. Aucun secret ; 63 fichiers en LF et 4 vides. Ce rapport ajoutera une 68e entrée. | ✅ |
| 10 | Documentation : README cohérent, `docs/11` à jour, ADR-007 présent et accepté, chemins de `docs/00` existants | Lecture de `README.md`, `docs/11`, `docs/07` ; test d'existence de chaque chemin cité dans `docs/00-INDEX.md` | README : n'annonce rien comme fonctionnel (« Le pipeline n'est pas encore implémenté … **Non implémenté à ce stade** : … ») ; défauts mineurs (D11). ADR-007 : « **Statut :** Accepté (2026-09-16) ». `docs/00` : tous les chemins existent, sauf `../reports/validations/` (créé par ce rapport) et `../logs/decisions.log` (attendu au Jalon 4 ; le dossier `logs/` existe). **`docs/11` n'est pas à jour** : « Dernière mise à jour : 2026-09-16 (soir) » ; il indique encore « Environnement Python … installation lancée (J-20260916-017), à confirmer » alors que l'installation est confirmée (J-20260917-001 : pandas 3.0.5 … pytest 9.1.1) ; « Phase : préparation terminée » contredit la ligne « … pull request à ouvrir … En cours » ; la fin des squelettes, le correctif du test du dashboard et la vérification en cours n'y figurent pas. | ❌ |

## Contrôles transverses

- **Tests :** `pytest -q` complet : 29 réussis, 38 ignorés, 0 échec (4,14 s). Aucun fichier n'est créé par les tests.
- **Traçabilité :** audits conformes sur les deux jours, mais 3 écarts non détectés par l'audit automatique (D1, D2). Les 10 échecs d'outils du 16/09 sont des commandes d'exploration (`ls` sur des dossiers pas encore créés, sortie 2) ou des imports faits avant la fin de l'installation. Les échecs d'import sont mentionnés dans les entrées 019 à 022 ; les `ls` ne font pas l'objet d'une entrée ÉCHEC, ce qui est acceptable pour des lectures. Le refus de 11:24:21 le 17/09 est le mien (voir critère 8) ; il est consigné dans J-20260917-005.
- **Valeurs en dur :** 1 défaut (D3) et 1 conflit de sources (D6).
- **Cohérence config ↔ docs :** conforme. Nuance : `docs/03` a été aligné sur le code après coup (J-20260916-025). Sa conformité au code est donc en partie construite, et l'ajout de `predict_threshold_baseline` a précédé sa documentation, contrairement à `docs/03` (« toute modification de signature doit d'abord être répercutée ici »). C'est désormais régularisé.
- **Périmètre (cahier §3) :** aucune fonctionnalité hors périmètre. Le dashboard est une page squelette sans authentification, graphique ni page supplémentaire ; l'installation de `gh` relève de l'outillage.
- **Conventions :** type hints et docstrings présents sur toutes les fonctions publiques. Deux `print(..., file=sys.stderr)` dans `src/` (D7).
- **Git :** aucun commit encore. La conformité des messages (Conventional Commits, trailers `Agent`/`Jalon`/`Journal`) sera à vérifier à l'itération 2 ou sur la PR.
- **Observations sans défaut :**
  - `RANDOM_STATE = 42` n'était documenté nulle part avant la synchronisation de `docs/03`. La section ml-engineer de `config.py` a été remplie par le data-engineer sur brief (J-20260916-018/019) : la ml-engineer devra confirmer ces valeurs au Jalon 3.
  - La docstring de `add_threshold_distance` et son test ignoré retiennent un « écart signé à la borne critique la plus proche », alors que `docs/02` §3 laisse le choix « binaire ou continue ». Ce n'est pas une décision ouverte listée, mais le choix est à confirmer au Jalon 2.

## Défauts à corriger

« Bloquant » = à corriger avant la fusion de la PR (G2). La PR peut être ouverte en brouillon avant correction.

| # | Défaut | Preuve | Agent propriétaire | Bloquant ? |
|---|---|---|---|---|
| D1 | Heures erronées non corrigées : J-20260916-023 (en-tête 21:15, écrite à 20:43:37) et J-20260916-025 (en-tête 21:24, écrite à 20:48:33). J-20260916-026 affirme à tort que les heures 019 à 023 sont exactes. Correction attendue : nouvelle entrée « Corrige J-20260916-023, 025 et 026 », sans réécriture. | `actions.jsonl` l. 242 et 274 (Edit du doc-keeper sur le journal) ; dernière action du doc-keeper le 16/09 à 20:48:41 ; horodatage du fichier à 21:11:14, antérieur à 21:15 et 21:24 | orchestrateur (entrée correctrice) ; entrées d'origine : doc-keeper | Oui |
| D2 | Modification non tracée de `logs/agents/journal/2026-09-16.md` à 21:11:14, alors que la dernière écriture tracée date de 20:56:27 et qu'aucune action n'est enregistrée entre 20:56:27 et 21:18:59. Impossible d'exclure une réécriture des entrées écrites par Edit ou par script. | `stat` : `2026-09-16 21:11:14.699 +0100` ; `actions.jsonl` l. 285 et 286 | orchestrateur (explication journalisée ; humain si édition ou restauration manuelle) | Oui, levé par l'explication |
| D3 | Valeur de repli codée en dur dupliquant la config : `_FALLBACK_RAW_DATA_PATH = "data/raw/IoTpond1.csv"`, chemin relatif au répertoire courant, branche morte. | `src/main.py:147-165` ; `conventions-code.md` §Configuration | automation-engineer | Oui |
| D4 | `docs/11` pas à jour : statut de l'environnement Python dépassé, phase « préparation terminée » contradictoire, squelettes, correctif et vérification absents. | `docs/11` l. 4, 8, 31, 33 ; J-20260917-001 | doc-keeper | Oui |
| D5 | Cause structurelle de D1 : le doc-keeper (`tools: Read, Grep, Glob, Write, Edit`) ne peut pas lire l'horloge système, pourtant exigée par `tracabilite.md` (« `date +%H:%M` ou `Get-Date` »). | `.claude/agents/doc-keeper.md` (en-tête) | orchestrateur (configuration des agents ; G1 si la gouvernance change) | Non, mais à traiter avant la prochaine délégation au doc-keeper |
| D6 | Défaut littéral `log_path = "logs/decisions.log"`, relatif au répertoire courant : imposé par `docs/03`, contraire à `conventions-code.md`, alors que `config.DECISIONS_LOG_PATH` existe. | Signature `inspect` ; `docs/03` l. 170 ; `conventions-code.md` l. 11-12 | doc-keeper + automation-engineer (arbitrage) | Non (avant le Jalon 4) |
| D7 | `print(..., file=sys.stderr)` dans `src/`, contraire à « Pas de `print` dans `src/` », mais cohérent avec l'exigence de `docs/03` (« message clair sur stderr »). | `src/main.py:225,228` ; `src/decision_engine.py:110` | automation-engineer (arbitrage conventions ↔ `docs/03`) | Non |
| D8 | `guard_git.py` : la liste `READ_ONLY` exclut toute forme de `git branch` (y compris `--show-current`), ainsi que `remote -v`, `rev-list`, `merge-base`… Résultat : faux positifs pour les sous-agents et ligne `refuse` parasite dans le journal automatique. Cohérent avec `git-workflow.md` §1, qui porte donc la même limite. | Refus du 17/09 à 11:24:21 ; `guard_git.py` l. 25-28 | orchestrateur (hook et règle) | Non |
| D9 | `docs/10` §7 présente la permission `deny` comme garantie pour `data/raw/` et `actions.jsonl`. Or `deny` ne couvre que les outils d'édition : une écriture par shell n'est pas bloquée, et la limite du §11 n'est citée que pour le qa-validator. | `settings.json` : `Edit(...)` seulement ; `docs/10` l. 128-129, 185 | doc-keeper | Non |
| D10 | Le test de fumée du dashboard lit le texte du bandeau dans la constante de l'application : s'il était modifié, le test resterait vert. La formulation R5 n'est pas vérifiée de façon indépendante. | `tests/test_dashboard.py` : `from dashboard.app import ACTUATOR_BANNER` | dashboard-developer | Non |
| D11 | README : le bloc « Structure » omet `models/`, et le statut reste daté du 2026-09-16. | `README.md` l. 9, 13-22 | doc-keeper | Non |

## Points à trancher par l'humain

1. **G4 — `data/raw/` :** créer le dossier et y déposer `IoTpond1.csv` avant le Jalon 1 (les agents n'y ont pas le droit). Décider aussi si `data/raw/.gitkeep`, prévu par `.gitignore` mais absent, doit être versionné pour qu'un clone ait le dossier.
2. **D2 :** si le journal du 16/09 a été ouvert, modifié ou restauré manuellement vers 21:11, le signaler à l'orchestrateur pour qu'il le journalise.
3. **G1 — conflit de sources** entre `docs/03` et `conventions-code.md` : défaut littéral de `log_decision` (D6) et messages d'erreur par `print` sur stderr (D7).
4. **G1 — gouvernance :** donner au doc-keeper un moyen de lire l'heure système (D5).
5. **R13 — dépôt public :** confirmer avant le push la publication de `logs/agents/actions.jsonl` (82 lignes contenant des chemins locaux `C:/Users/Admin/…` et des identifiants de session) et de l'adresse de l'identité git (présente dans `.claude/rules/git-workflow.md` et dans le journal du 16/09). Rien de secret n'a été détecté.
6. **ADR-007 :** la citation d'acceptation reprend la demande initiale, qui déléguait le choix des conventions. Confirmer qu'elle vaut acceptation du contenu rédigé. Non bloquant.
7. **G2 :** décision de fusion après correction des défauts D1 à D4 (itération 2).

---

## Itération 2 — 2026-09-17 12:51

- Validateur : qa-validator · Délégation J-20260917-016 · Corrections vérifiées : J-20260917-006 à 015
- Verdict proposé : **NON VALIDÉ** — **un seul défaut bloquant subsiste (N1)**, purement documentaire : l'entrée J-20260917-008, qui explique D2, cite une preuve fausse (fichier « en CRLF »). Une seule entrée « Corrige J-20260917-008 » suffit à le lever. Plus aucun critère ne serait alors ❌ : le verdict deviendrait **VALIDÉ AVEC RÉSERVES** (réserves ci-dessous), sans nouvelle vérification complète. La limite de 2 itérations est atteinte : décision à l'humain (G2).

Méthode : les commandes de l'itération 1 ont été relancées depuis la racine du projet avec `venv/Scripts/python.exe`. Aucun garde-fou n'a été lancé avec des commandes de test. D8 a été contrôlé en appelant la fonction pure `is_read_only` de `guard_git.py`, sans `main()` ; aucune ligne `refuse` n'a été ajoutée à `actions.jsonl`.

### Vérification des corrections

| Défaut | Correction annoncée | Preuve vérifiée | Statut |
|---|---|---|---|
| D1 | J-20260917-007 corrige les heures de J-20260916-023 et 025 ainsi que l'affirmation de 026 | L'entrée existe, ajoutée à 12:23:55 (`actions.jsonl` l. 349). Les heures réelles citées (20:43:37 et 20:48:33) correspondent aux lignes 242 et 274 ; « seules 019 à 022 le sont » est exact, à une minute près pour 020 et 021. Les entrées d'origine sont intactes. | ✅ corrigé |
| D2 | J-20260917-008 : sauvegarde automatique de VS Code ; « les 220 lignes sont en fins de ligne CRLF » | **Explication corroborée** : `files.autoSave: "afterDelay"` est présent dans les réglages utilisateur de VS Code, fichier modifié à `2026-09-16 21:12:56`, soit 102 s après l'écriture non tracée de 21:11:14. L'utilisateur était donc actif dans l'IDE. Numéros 001 à 027 et ordre d'ajout inchangés ; début des 9 entrées du 16/09 ajoutées par shell identique au texte tracé ; taille inchangée depuis l'itération 1 (36 120 octets). **Mais la preuve CRLF est fausse** : comptage des octets `CRLF= 0 LF seuls= 220` ; `git ls-files --eol` → `w/lf`, déjà `w/lf` à 11:40 en itération 1. La mesure vient de `grep -c $'\r' $F` (`actions.jsonl` l. 348), qui compte toutes les lignes dans cet environnement : le témoin `src/config.py`, entièrement en LF, donne `117 / 117`. | ⚠️ écart expliqué, preuve journalisée fausse → **N1** |
| D3 | J-20260917-011 : repli supprimé, 2 tests ajoutés | `grep -rn "FALLBACK\|data/raw/IoTpond1" src dashboard` : aucun résultat. `_default_input_path()` importe seulement `config.RAW_DATA_PATH` (`src/main.py:33-45`). Nouveaux tests : `test_default_input_path_matches_config` (défaut `--input` == `str(RAW_DATA_PATH)`) et `test_default_input_path_raises_if_config_unavailable` (`sys.modules["src.config"] = None` → `ImportError`). Tous deux passent. | ✅ corrigé |
| D4 | J-20260917-012 et 015 : `docs/11` mis à jour | Dernière mise à jour 2026-09-17 ; environnement Python « Levé (J-20260917-001 …) » ; la phase « mise en place en cours de finalisation » n'est plus contradictoire ; réserves, `gh` et décisions humaines reportés. Restent 3 inexactitudes mineures (réserve R3). | ✅ corrigé, réserve mineure |
| D5 | J-20260917-009 : l'agent sans shell lit l'heure dans `actions.jsonl` | Règle ajoutée dans `tracabilite.md` l. 38 et `doc-keeper.md` l. 53. Appliquée dans J-20260917-012 et 015 : les `ts` cités (12:27:48, 12:37:46) existent (l. 378, 389), mais ils ont été lus **avant** les dernières écritures du doc-keeper (journal écrit à 12:29:48 et 12:38:24), soit un écart de 2 min au plus (réserve R4). | ✅ corrigé, réserve mineure |
| D6 | Reporté au Jalon 4 | Consigné dans `docs/11`, « Décisions en attente », ligne D6 (propriétaires doc-keeper + automation-engineer, bloque le Jalon 4). La signature de `log_decision` est inchangée. | ⏳ réserve R2 |
| D7 | J-20260917-009 : exception pour les messages CLI sur stderr | `conventions-code.md` l. 19 : « Seule exception : les messages destinés à l'utilisateur d'un point d'entrée en ligne de commande … écrits sur `sys.stderr` (`docs/03`) ». Les usages de `src/main.py` et `src/decision_engine.py` entrent dans cette exception. | ✅ résolu |
| D8 | J-20260917-009 : lecture seule git élargie ; `git-workflow.md` §1 | `is_read_only` sur 42 cas : **42/42 conformes** (formes de consultation de `branch`, `tag -l`, `remote -v/get-url`, `config --get/--list`, `stash list/show`, `reflog`, `rev-list`, `merge-base`, `for-each-ref`, `show-ref` autorisées ; `branch nouvelle`, `branch -v nouvelle`, `branch -d/-D/-m`, `tag v1/-a/-d`, `remote add/set-url`, `config user.name x`, `--unset`, `stash`/`drop`/`pop`, `reflog expire/delete`, `commit`, `push`, `switch -c`, `add`, `fetch` refusés). `git branch --show-current` a ensuite été exécuté par le qa-validator sans refus. §1 de la règle aligné. | ✅ corrigé |
| D9 | J-20260917-012 : `docs/10` §7 et §11 | §7 : les deux lignes `deny` passent en « ⚠️ … ne couvre pas un sous-processus qui écrit lui-même dans le fichier ». §11 : limite étendue à tous les agents. Le refus de `mkdir data/raw` attribué à `deny` est cohérent avec l'absence de toute trace (0 ligne `mkdir … data/raw`, 0 événement `PermissionDenied`), sans être démontré formellement. | ✅ corrigé |
| D10 | J-20260917-013 : texte du bandeau écrit littéralement dans le test | `tests/test_dashboard.py:25` : `REQUIRED_ACTUATOR_BANNER = "Actionneurs simulés — aucun matériel connecté"` ; plus aucun import de `dashboard.app`. Test actif vert. Le contrôle par mutation annoncé ne peut pas être rejoué (copie dans le scratchpad), mais la construction du test rend la détection effective. | ✅ corrigé |
| D11 | J-20260917-012 : README | `models/` ajouté au bloc « Structure » ; statut « État au 2026-09-17 (Jour 1) ». Le nombre de tests (29) est dépassé (réserve R3). | ✅ corrigé, réserve mineure |
| Décisions humaines | J-20260917-014 et 015 | R13 : `docs/08` indique « Publication confirmée par l'humain le 2026-09-17 … », statut Maîtrisé. `data/raw/.gitkeep` : « pas de `data/raw/.gitkeep` versionné, `data/README.md` suffit » (`docs/11` l. 30). ADR-007 : « contenu rédigé confirmé explicitement par l'humain le 2026-09-17 » (`docs/11` l. 62) ; l'ADR n'a pas été réécrit. | ✅ reportées |

### Critères réévalués

| # | Critère | Méthode / commande | Observé | Verdict (it. 1 → it. 2) |
|---|---|---|---|---|
| 1 | Structure et dossiers | `ls -d data/raw` ; existence des entrées de `docs/03` | `reports/validations/` existe. `data/raw/` est toujours absent (`No such file or directory`). L'humain a décidé (J-20260917-014) de le créer lui-même pour le Jalon 1, sans `.gitkeep` versionné. Le critère reste non satisfait techniquement, mais son traitement est tranché par l'autorité compétente (G4). | ⚠️ → ⚠️ (réserve R1) |
| 5 | Valeurs en dur | `ast` : littéraux numériques et chaînes de type chemin hors docstrings (`src/`, `dashboard/`) | Repli de `src/main.py` supprimé. Restent : `src/decision_engine.py:81` `'logs/decisions.log'` (défaut imposé par `docs/03`, conflit consigné D6) et `src/main.py:51` `'src/main.py'` (libellé `prog` d'argparse, pas un accès fichier). | ❌ → ⚠️ (réserve R2) |
| 7 | Traçabilité | Audits des deux jours ; croisement des heures avec `actions.jsonl` ; numérotation et ordre ; débuts des entrées ajoutées par shell ; comptage des octets CRLF/LF | Audits ✅ CONFORME (16/09 : 27 entrées, 221 actions ; 17/09 : 16 entrées, 113 actions, 0 fichier non cité, 0 entrée mal formée). D1 corrigé. D2 expliqué et corroboré. Numérotation du 17/09 continue de 001 à 016, ordre du fichier = ordre d'ajout (013, 011, 012 écrites à 12:26:48, 12:27:03 et 12:29:48). Débuts des 18 entrées ajoutées par shell (16 et 17/09) identiques au texte tracé. Heures d'en-tête du 17/09 à 2 min près. **Mais J-20260917-008 journalise une preuve fausse** (220 lignes « en CRLF », en réalité 0) : c'est contraire à la règle n° 3 (« résultat prouvé »), et une correction par nouvelle entrée est requise, comme pour J-20260916-026 en itération 1. | ❌ → ❌ (N1) |
| 10 | Documentation | Lecture de `docs/11`, `README.md`, `docs/10`, `docs/08` | `docs/11` et le README reflètent l'état réel pour l'essentiel. ADR-007 accepté et confirmé. Les chemins de `docs/00` existent (hors `logs/decisions.log`, attendu au Jalon 4). Restent : « 29 tests réussis » (`docs/11` l. 8, README l. 9) au lieu de 31 ; D3 et D10 notés « délégué » (`docs/11` l. 68, 71) alors qu'ils sont corrigés (J-20260917-011, 013) ; « Prochaine action » ne mentionne ni la PR ni G2. | ❌ → ⚠️ (réserve R3) |

Critères 2, 3, 4, 6, 8 et 9 : contrôlés à nouveau, toujours ✅ (voir ci-dessous).

### Contrôles transverses (complets)

- **Tests :** `venv/Scripts/python.exe -m pytest -q` → `31 passed, 38 skipped in 4.25s`, 0 échec. Analyse `ast` : 31 tests actifs, tous avec au moins un `assert` ou `pytest.raises`, aucun trivial ; 38 `skip` référencés (jalon + règle ou critère).
- **Traçabilité :** `audit_trace.py --date 2026-09-16` → ✅ CONFORME (code 0) ; `--date 2026-09-17` → ✅ CONFORME (code 0). Échecs ou refus du 17/09 : mon refus de 11:24 (consigné dans J-20260917-005, cause corrigée par D8) et un échec du dashboard-developer à 12:26:14 (première exécution de son script de mutation dans le scratchpad, relancé avec succès), non mentionné dans J-20260917-013 (observation O3).
- **Contrats et squelette :** 19 signatures conformes à `docs/03` (18 identiques, 1 équivalente en valeur) ; 16 fonctions métier et `run_pipeline` lèvent `NotImplementedError` référencé.
- **Cohérence config ↔ docs :** `THRESHOLDS docs/03 == config : True`, extrait identique caractère pour caractère ; `TEMPERATURE_BOUNDS` et `PH_BOUNDS` conformes ; cahier §5 inchangé et conforme.
- **Valeurs en dur :** voir critère 5.
- **Périmètre :** aucune fonctionnalité hors cahier §3 ajoutée par les corrections (configuration, règles, tests et documentation seulement).
- **Git :** `git branch --show-current` → `chore/mise-en-place-projet` ; `HEAD = origin/main = 00cfe47` ; `git log --oneline origin/main..HEAD` vide. `git status` : 68 entrées non suivies (67 + ce rapport), aucune modification de fichier suivi. `check-ignore` : `venv/` (`.gitignore:2`), `data/raw/*` (`.gitignore:11`), `__pycache__/` (`.gitignore:4`), `.pytest_cache/` (`.gitignore:6`). Aucun fichier de `data/raw/`, de `venv/` ni aucun artefact régénérable. Aucun fichier > 5 Mo (le plus gros : `logs/agents/actions.jsonl`, 146 017 octets). Aucun jeton ni clé privée réels ; les seules correspondances viennent des commandes du qa-validator, qui citent les motifs. Fins de ligne : tout en LF, sauf 4 `.gitkeep` vides.
- **Commits :** aucun encore. La conformité des messages (Conventional Commits, trailers `Agent`/`Jalon`/`Journal`) reste à contrôler sur la PR.

### Défauts à corriger

| # | Défaut | Preuve | Agent propriétaire | Bloquant ? |
|---|---|---|---|---|
| N1 | J-20260917-008 donne comme preuve « les 220 lignes sont en fins de ligne CRLF » et annonce une normalisation LF au commit. C'est faux : le fichier ne contient aucun CRLF, et la mesure est un artefact de `grep -c $'\r'`. Correction attendue : une entrée « Corrige J-20260917-008 » qui retire cette preuve et fonde l'explication sur des éléments vérifiés (réglage `files.autoSave`, activité VS Code à 21:12:56, numérotation, ordre et débuts d'entrées inchangés). Sans réécrire l'entrée d'origine. | Comptage d'octets : `CRLF= 0 LF seuls= 220` ; `git ls-files --eol` → `w/lf` (11:40 et 12:4x) ; témoin `grep -c $'\r$' src/config.py` → `117 / 117` sur un fichier LF ; commande de 12:23:00 (`actions.jsonl` l. 348) | orchestrateur | Oui (avant fusion) |

### Réserves (non bloquantes)

| # | Réserve | Agent propriétaire | Échéance |
|---|---|---|---|
| R1 | `data/raw/` et `IoTpond1.csv` absents (critère 1) ; décision : pas de `.gitkeep` versionné | humain (G4) | Avant l'ouverture du Jalon 1 |
| R2 | Défaut littéral `log_path = "logs/decisions.log"` (D6) : arbitrer entre `docs/03` et `conventions-code.md`, puis aligner le code et la doc | doc-keeper + automation-engineer (arbitrage orchestrateur/G1) | Avant l'implémentation du Jalon 4 |
| R3 | `docs/11` et README : 29 → 31 tests ; D3 et D10 à passer de « délégué » à « corrigé » ; « Prochaine action » à aligner sur PR/G2 ; verdict de l'itération 2 à reporter | doc-keeper | Prochaine mise à jour de `docs/11` (suite à ce rapport), au plus tard à l'ouverture du Jalon 1 |
| R4 | Heure des entrées sans shell lue avant les dernières écritures (J-20260917-012 : `ts` 12:27:48 pour une écriture à 12:29:48 ; J-20260917-015 : 12:37:46 pour 12:38:24), contrairement à « juste après ta dernière écriture » | doc-keeper | Prochaine entrée de journal |
| R5 | `RANDOM_STATE = 42` (et section ml-engineer de `config.py` remplie par le data-engineer) à confirmer par son propriétaire | ml-engineer | Début du Jalon 3 |
| R6 | Choix d'un « écart signé » continu dans `add_threshold_distance` (docstring et test ignoré), alors que `docs/02` §3 laisse « binaire ou continue » | data-engineer | Début du Jalon 2 |
| R7 | Conformité des commits (Conventional Commits, trailers `Agent`/`Jalon`/`Journal`, aucun fichier interdit dans le staging) à contrôler sur la PR | orchestrateur (contrôle qa-validator sur la PR) | Avant fusion |

Observations sans action :
- **O1 :** l'attribution du refus de `mkdir data/raw` à la règle `deny` (`docs/10` §7) est cohérente avec l'absence de trace, mais n'est pas démontrée.
- **O2 :** le contrôle par mutation de D10 n'est pas rejouable (copie hors dépôt), mais la construction du test suffit.
- **O3 :** la première exécution ratée du script de vérification du dashboard-developer (12:26:14) n'est pas citée dans J-20260917-013 (règle n° 4, échecs à journaliser).

### Points à trancher par l'humain

1. **G2 — limite de 2 itérations atteinte :** accepter la fusion dès que l'entrée « Corrige J-20260917-008 » (N1) est ajoutée, sous les réserves R1 à R7, sans troisième vérification complète ; ou exiger une vérification ciblée de cette seule entrée.
2. **Conseil (J-20260917-008) :** ne pas ouvrir les journaux (`logs/agents/`) dans l'IDE en écriture, la sauvegarde automatique étant active.
